import { IndextocharPipe } from './indextochar.pipe';

describe('IndextocharPipe', () => {
  it('create an instance', () => {
    const pipe = new IndextocharPipe();
    expect(pipe).toBeTruthy();
  });
});
